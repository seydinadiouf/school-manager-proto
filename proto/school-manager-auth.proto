syntax = "proto3";

package proto;

import "google/api/annotations.proto";
import "user.proto";

option go_package = "./proto";


service SchoolManagerAuthService {

  rpc signIn(SignInRequest) returns (SignInResponse){
    option (google.api.http) = {
      post: "/login"
      body: "*"
    };
  }
}